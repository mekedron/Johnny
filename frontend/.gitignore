node_modules
# pnpm's content-addressable store can land here when `pnpm install` runs
# inside the bind-mounted dev container (store-dir resolves to /workspace).
# It's build cache, never source — keep it out of git and the image.
.pnpm-store/

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
