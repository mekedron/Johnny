# Ralph Progress Log

This file tracks progress across iterations. Agents update this file
after each iteration and it's included in prompts for context.

## Codebase Patterns (Study These First)

- **Run pytest in the worker container** (not api): the api image is built from production-shape Dockerfile (`COPY . .`) and has no bind mount unless launched via `./run-dev.sh`. The `worker` service in dev mode bind-mounts `./backend` to `/workspace`, so host edits are visible immediately. The image's `/opt/venv` was built via `uv` and has no `pip`; install dev deps with `uv pip install --python /opt/venv/bin/python pytest pytest-asyncio aiosqlite ruff mypy types-PyYAML`, then run `docker compose exec -w /workspace worker python -m pytest …`.
- **`PipelineConfig` is `@dataclass(frozen=True, slots=True)`**: add fields with sensible defaults so existing call-sites keep working without modification. Document the *why* in a docstring directly under the field — the file is the canonical config reference for the whole pipeline.
- **Barge-in has two paths**: fast VAD-driven (Johnny-ze3, in `_utterances`) cuts TTS within ~200 ms of speech onset; slow classifier (Johnny-di9, in `_classify_barge_in_intent`) runs post-utterance as a fire-and-forget task spawned at `_transcribe_and_emit`. The slow path is observability + edge-cases, not the user-facing budget — fail-open is the right default when its LLM is slow.
- **STT silence hallucinations belong at the adapter, not the pipeline gate**: Whisper segments expose `no_speech_prob` — the model's own probability that the audio was silence. Filtering on that signal inside `app/providers/faster_whisper_stt.py` catches *novel* hallucinations ("Does Olam A.P.I.", Welsh nonsense) that a curated stoplist in `pipeline.py` can never anticipate. The pipeline's `DEFAULT_NOISE_STOPLIST` (Johnny-ckz.14) handles known patterns ("you", "thank you", filler tokens); the adapter's `no_speech_threshold` handles everything else. Defence in depth — keep both.
- **Provider adapters live in the api image, not just meet-worker**: the in-process `BrowserPipelineSpec` (`app/services/browser_pipeline_runner.py`) means `/playground` runs the FULL voice pipeline inside the api container — so any STT-adapter change has to land in BOTH images (api + meet-worker). The api image is COPY-baked (`Dockerfile`), so after editing `app/providers/*.py` run `docker compose build api && docker compose up -d api` before exercising the playground; the worker has a bind mount and picks up host edits live, but the api does not.
- **`provider_credentials.is_active` per kind is touched in only two places**: (1) `app/api/providers.py` — `activate_provider` (deactivates same-kind siblings then flips this row True), `deactivate_provider` (explicit user click), `create_provider` (always inserts inactive). `update_provider` does NOT touch `is_active` — schema-driven merges drop the field via `merged = {k: v for k, v in baseline.items() if schema.field(k) is not None}` because the schemas don't declare `is_active`. (2) `app/services/providers_seed.py` — only mutates when `JOHNNY_PROVIDERS_FILE` exists. Missing file = early return = no-op. INSERT_ONLY mode skips existing rows entirely so `is_active` is preserved across restart. OVERWRITE mode is the only path that intentionally clears the flag (when the file explicitly lists `is_active: false` for an existing identity, per the documented export→edit→import roundtrip). Symmetric across LLM/STT/TTS — no kind-specific branch anywhere.
- **`ProviderSchema.tips` is the in-UI knowledge-capture surface**: when you need to put operator know-how (latency rules of thumb, knob trade-offs, cost guidance) directly inside `/providers`, edit the adapter's `field_schema()` and pass `tips=(ProviderTip(topic=..., body=...), ...)`. The schema serialises tips automatically; the frontend renders them as a "Latency & tuning tips" section below the form fields. Topic is a short headline; body is 1-2 sentences with a concrete number where possible. Adapter file is the canonical home — don't fork knowledge into a separate docs file (it goes stale next to the knobs it's about). After editing, rebuild the api image (`docker compose build api && docker compose up -d api`) — the COPY-baked Dockerfile won't pick up the change otherwise.
- **`_utterance_count` is NOT a valid turn_id from inside the response loop**: transcription (`_transcribe_and_emit`) and response (`_respond_to_transcript`) run as concurrent tasks (Johnny-har). By the time the response loop dequeues transcript N and starts router/answer/tts, the transcribe loop may have incremented `_utterance_count` past N. For activity-log-style cross-stage correlation (Johnny-ckz.7), stamp `(turn_id, anchor_ms)` in a `dict[int, tuple[int, int]]` keyed by `id(transcript)` at STT-finalise time, then `pop()` it when the response loop dequeues. Falls back to `_utterance_count + now` for transcripts injected via `feed_text` (no transcribe-loop stamp). Same caveat applies to ANY new code that needs "what turn am I in?" from the response side — don't read `_utterance_count` there.
- **The pipeline event bus is multiplexed**: every new event TYPE shares one Redis pub/sub channel per session and one `InMemoryEventBus.snapshot()` in tests. When adding a new type (e.g. Johnny-ckz.7's `PipelineTiming`), the existing tests that assert `[e.type for e in bus.snapshot()] == [...]` will fail because the new events leak in. The repo convention: filter at the test site — `[e.type for e in bus.snapshot() if e.type != "<new_type>"]` — rather than gating the publish at the source. Observability events (timings, traces) should publish unconditionally so they show up in the activity log; only the assertion list needs updating.
- **The migrate sidecar image must be rebuilt alongside api+worker**: `docker-compose.yml` defines `api`, `worker`, and `migrate` as three separate services with three separate images, all built from the same `backend/Dockerfile`. `docker compose build api` only rebuilds the api image — migrate keeps its old layer with whichever migrations it had at last build. After adding a new alembic revision, run `docker compose build api migrate worker` (or `docker compose build` for all) so the migrate sidecar can find your new `0008_*.py` file at startup. Symptom of missing this: migrate fails with `Can't locate revision identified by 'NNNN'` even though the file exists on the host and the api image has it.
- **Cartesia TTS uses `POST /tts/bytes` with `container=raw, encoding=pcm_s16le, sample_rate=16000`**: the simplest of Cartesia's three streaming surfaces (WebSocket / SSE / raw HTTP). The chunked HTTP response body IS the PCM stream — no SSE framing, no WebSocket framing, no resampling. Mirrors the ElevenLabs pattern (`response.aiter_bytes` → carry-byte alignment → yield). Auth header is `X-API-Key: <key>` (Cartesia accepts both X-API-Key and Authorization: Bearer — X-API-Key is idiomatic in their docs). Mandatory `Cartesia-Version` header pinned to `2026-03-01` (current at 2026-06-07 fetch date); the API ships breaking changes between dated versions so this pin is load-bearing. Voice IDs are UUIDs from `GET /voices`; paginated via `starting_after` / `has_more`. Sonic 3.5 advertises sub-200 ms time-to-first-audio from US/EU — comparable to ElevenLabs Flash v2.5.
- **Dual-mode pipeline routing lives at BOTH session entry points (Johnny-ckz.17)**: the meet-worker reads `JOHNNY_PIPELINE_MODE` env var (default `split`) inside `johnny.meet_worker.pipeline_runner.build_and_run_pipeline`, while the in-process browser runner reads `BrowserPipelineSpec.pipeline_mode` inside `assemble_browser_pipeline`. Both delegate to either `VoicePipeline` (split: STT→LLM→TTS) or `UnifiedVoicePipeline` (unified: a single `S2SProvider`). The router lives inside the assembler — there's no codepath that constructs `VoicePipeline` directly without first checking the mode. Persistence is the singleton `pipeline_settings` table (id=1, default 'split') read once at session start by `resolve_pipeline_mode`. The `docker_launcher` forwards the persisted mode into the worker's env via `LaunchContext.pipeline_mode` so the meet-worker stays SQLAlchemy-free.
- **The unified pipeline's events loop blocks on `queue.get()` forever unless the session is closed**: `S2SSession.events()` is an async iterator that exits only when the queue receives a `None` sentinel from `close()`. The capture loop's finally calls `commit_user_turn`, which queues response events; the events loop drains them but then blocks on the next `get`. The fix in `UnifiedVoicePipeline.run` is a two-phase teardown: after the first wait (capture EOF or stop signal) gives a 500 ms drain window, then `await session.close()` (queues None), then a 1 s wait for the events loop to exit naturally on the sentinel, then cancel any stragglers. Without this, AgentSpoke + final transcripts get cancelled mid-flight when the run loop tears down.
- **`ProviderInstance` is `_ProviderBase` not a `A | B | C | D` union**: originally a `STTProvider | LLMProvider | TTSProvider` union, but Johnny-ckz.17 added a fourth ABC (`S2SProvider`) in a separate module to keep import isolation. PEP 604 forward refs (`"S2SProvider"`) don't work in runtime type unions — `TypeError: unsupported operand type(s) for |: 'types.UnionType' and 'str'`. The fix: alias `ProviderInstance = _ProviderBase`, which is the common supertype of every provider ABC. Runtime `isinstance(x, ProviderInstance)` still works correctly.
- **`ck_provider_credentials_kind` CHECK was missed in Johnny-ckz.17 (Johnny-ckz.21)**: the initial schema migration 0001 created `CHECK (kind IN ('stt', 'llm', 'tts'))`. Johnny-ckz.17 widened the Python `ProviderKind` enum to include `s2s` but the corresponding DB CHECK was overlooked because the model declares `native_enum=False` + no explicit `CheckConstraint`. Symptom: inserting an S2S row failed with an `IntegrityError`, which the `create_provider` API surfaced as a misleading "display name already exists" (the catch-all branch). Migration 0010 drops + recreates the constraint with `('stt', 'llm', 'tts', 's2s')`. The downgrade tries to put the 3-value constraint back, which correctly FAILS when an s2s row exists — that's load-bearing data-loss prevention, not a bug. PG transactional DDL rolls the whole downgrade back to the new constraint, leaving the DB at 0010. Lesson for future kind widenings: grep the `versions/` tree for `_in_list("kind"` / similar CHECK helpers, not just the SQLAlchemy model.
- **Per-mode pipeline_mode pickup at session start (Johnny-ckz.21)**: Johnny-ckz.17 added the `pipeline_settings` table + `resolve_pipeline_mode` helper and made the assembler dispatch on `BrowserPipelineSpec.pipeline_mode` / `LaunchContext.pipeline_mode`, but neither the browser session entry points (`_build_spec_playground`, `_build_spec_from_event`) nor the scheduler (`_persist_session` building `LaunchContext`) actually called `resolve_pipeline_mode`. The persisted toggle therefore had no effect — every session got the dataclass default ('split') regardless of the operator's choice. Wired in this iteration: API browser-sessions read the singleton row and pass `pipeline_mode=mode.value` to both the spec and the `playground_overrides` snapshot (so the UI's "Pipeline" chip can render before the assembler logs), and the scheduler does the same for Meet sessions. Pattern: any new global pipeline toggle has THREE places to wire (resolver + spec/ctx + overrides snapshot) — miss any one and the toggle's a no-op.
- **Gemini Live model names diverge from marketing aliases (Johnny-ckz.20)**: the Google docs frequently surface a `gemini-live-2.5-flash-native-audio` alias, but the v1beta WebSocket endpoint only recognises the raw `gemini-2.5-flash-native-audio-*` form — sending the aliased name yields a 1008 policy violation ("models/... is not found for API version v1beta, or is not supported for bidiGenerateContent"). The authoritative list comes from `GET /v1beta/models?key=...&pageSize=200` filtered to entries with `bidiGenerateContent` in `supportedGenerationMethods` — at fetch date 2026-06-07 that yielded only `gemini-2.5-flash-native-audio-latest` (stable), `gemini-2.5-flash-native-audio-preview-09-2025` (legacy preview), `gemini-2.5-flash-native-audio-preview-12-2025` (preview), and `gemini-3.1-flash-live-preview` (newest). Encode the default as the `-latest` form and surface those four exact names in the dropdown — anything else fails at session open with a misleading error.
- **Gemini Live ignores sine-wave / silence input for turn detection (Johnny-ckz.20)**: with the default `automatic_activity_detection` enabled, the server's VAD is tuned for human voice — a 440 Hz tone won't fire turn detection and `audioStreamEnd: true` is treated as a stream signal not a turn boundary, so a silent / synthetic-tone test just hangs waiting for a response. The two paths that DO work: (a) send `clientContent` with text (not part of our `S2SSession` ABC, so unavailable to the unified pipeline), or (b) disable server VAD via `realtime_input_config.automatic_activity_detection.disabled=true` in setup and drive turns explicitly with `activityStart` / `activityEnd`. The adapter exposes a `disable_server_vad` option for (b), used in the live integration tests so they're deterministic without needing a real-speech WAV. Production users keep the default (server VAD) — it's more accurate for actual voice input than client-side activity markers.
- **Gemini Live has no client-side "cancel response" message (Johnny-ckz.20)**: unlike OpenAI Realtime which exposes `response.cancel`, Gemini Live's barge-in is entirely VAD-driven on the server. The way to interrupt mid-response is to send a NEW user turn — auto VAD picks up the speech, manual VAD reads `activityStart`, either way the server emits `serverContent.interrupted: true` and stops generation. The adapter's `session.interrupt()` sends `realtimeInput.activityEnd: {}` as a soft signal but the pipeline-side `UnifiedVoicePipeline.interrupt()` is what actually cancels playback (via `transport.cancel_playback`). Real-server confirmed: a fresh `send_audio` + `commit_user_turn` after the first audio frame triggers the interrupt path and a clean `S2SResponseCompleted` event arrives within seconds.
- **OpenAI Realtime GA reshape: `session.update` REQUIRES `rate` on BOTH input AND output audio.format (Johnny-ckz.19)**: the May 2026 GA reshape nested audio config under `session.audio.input` / `session.audio.output` and renamed `modalities` → `output_modalities`. The docs example often shows the output format as just `{"type": "audio/pcm"}` without a rate — that's incomplete. The live API rejects it with "Missing required parameter: 'session.audio.output.format.rate'" only after the session is opened (not at WS connect time). Both input and output formats need `{"type": "audio/pcm", "rate": 24000}`. The minimum sample rate is 24 kHz; the GA endpoint rejects 16 kHz with "integer below minimum value". The pipeline-side adapter upsamples capture (16 → 24 kHz) and downsamples assistant audio (24 → 16 kHz) at the wire boundary so the rest of the pipeline stays at one rate.
- **OpenAI Realtime: model name pins to `gpt-realtime-2` (May 2026 GA flagship)** — siblings `gpt-realtime-mini` (cheaper), `gpt-realtime` (Aug 2025 GA, legacy). `gpt-4o-realtime-preview-2024-12-17` is DEPRECATED and rejects the GA session shape because it requires the flat pre-GA keys. Two new flagship voices are Realtime-exclusive: `marin` (warm/natural) and `cedar` (deeper/authoritative); the legacy 4o-realtime voices (alloy/ash/ballad/coral/echo/sage/shimmer/verse) still work. **`OpenAI-Beta: realtime=v1` header is REMOVED in GA** — sending it routes to the deprecated beta endpoint and gets rejected with "The Realtime Beta API is no longer supported". Authentication is just `Authorization: Bearer <api_key>`. Sessions cap at 30 min (`error.code=session_expired`); long meetings need a reconnect strategy not implemented yet.
- **OpenAI Realtime exposes a real client-side `response.cancel` for barge-in (Johnny-ckz.19)** — unlike Gemini Live which is VAD-only. The adapter's `interrupt()` sends BOTH `response.cancel` (stops generation) AND `input_audio_buffer.clear` (drops pending uncommitted user audio so the next turn starts clean). Server then emits `response.done` with `status: "cancelled"`, which the adapter maps to `finish_reason="interrupted"`. Server VAD path is also available: `input_audio_buffer.speech_started` arrives within ~200 ms of fresh user voice and the adapter eagerly emits an interrupted marker so the pipeline doesn't wait for the eventual `response.done(cancelled)` — that lets the in-flight assistant audio persist with the right finish_reason and stops new frame accumulation.
- **OpenAI Realtime API requires ≥100 ms of audio in the buffer before commit (Johnny-ckz.19)** — sending pure silence (all-zero PCM) followed by `input_audio_buffer.commit` yields "Error committing input audio buffer: buffer too small. Expected at least 100ms of audio, but buffer only has 0.00ms of audio." Live integration tests use a 1 s 440 Hz sine tone with `turn_detection="none"` so the buffer crosses the gate and the test drives the turn deterministically without depending on server VAD detecting speech in synthetic audio. Production users keep server_vad on — fine for real voice input.
- **Gemini Live treats every post-commit audio chunk as a NEW user activity (Johnny-ckz.22)** — in manual-VAD mode (`disable_server_vad=True`) the adapter sends `activityStart` on the first `send_audio` after every `activityEnd`. So if a harness keeps feeding silence frames to the session AFTER `commit_user_turn`, each frame opens a fresh activity and the server interprets that as a barge-in, cancelling the in-flight response. The unified-pipeline harness handles this by having its holding transport switch to yielding `b""` after the commit driver fires; the pipeline's `if not frame: continue` drops empty frames so they never reach `send_audio`. Capture stays alive (no EOF, no teardown) without re-feeding audio. Don't apply this transformation to OpenAI Realtime — it accepts post-commit audio as buffering for the next turn without re-firing.
- **`UnifiedVoicePipeline._capture_loop` only commits on capture EOF, never mid-stream (Johnny-ckz.22)** — when the harness wants to drive a deterministic turn against an S2S provider, it can't just feed audio and wait for the response: the pipeline calls `commit_user_turn` ONLY in the `finally` of its capture loop, after the capture iterator exits. To exercise an S2S session against a finite scripted timeline, the harness needs (a) a commit driver that calls `session.commit_user_turn()` from a side-task after the speaker's last speech event finishes + a small grace period, AND (b) a holding transport that keeps capture alive (yielding empty frames so the pipeline's drop-on-empty filter no-ops them) until the runner observes the artifact-side `S2SResponseCompleted` and signals stop. Without both, the response never arrives because either commit isn't sent (no fin), or the pipeline tears down before the response generates.

---

## 2026-06-07 - Johnny-ckz.22
- Extended the existing interrupt-reproduction harness (`johnny.e2e.interrupt`) to drive the unified S2S pipeline against the real OpenAI GPT-Realtime and Gemini Live APIs. The same `python -m johnny.e2e.interrupt` entry point now switches between split (legacy STT+LLM+TTS) and unified (single S2S provider) modes via `--mode`, picks the S2S adapter via `--provider`, and annotates the report with the entry-point surface (`--surface=meet|playground`) so operators can audit "playground + Meet parity" from a single run.
- Files changed:
  - `backend/johnny/e2e/interrupt/__main__.py` — added `--mode={split,unified}`, `--provider`, `--surface={meet,playground}`, `--voice-id`, `--server-vad` flags. Split the dispatch into `_run_split` (legacy paths) and `_run_unified` (new S2S harness). The unified path SKIPs cleanly with a clear stderr message + exit 0 when credentials are absent (per the bead's "cleanly skipped with clear reason" acceptance).
  - `backend/johnny/e2e/interrupt/s2s_scenarios.py` (new) — `S2SScenario` dataclass + three concrete scenarios: `s2s_open_and_receive_audio` (smoke), `s2s_barge_in_via_session_interrupt` (calls `pipeline.interrupt()`; works as real cancel on OpenAI Realtime, soft hint on Gemini Live), `s2s_barge_in_via_new_user_turn` (fresh `send_audio` + `commit_user_turn` mid-response — the canonical Gemini barge-in shape, also works on OpenAI). Each scenario has a `runner_timeout_s` ≥ 30 s and adapter-tuned `interrupt_latency_budget_s` (8 s for session_interrupt to accommodate Gemini's no-cancel path; 3 s for new_user_turn).
  - `backend/johnny/e2e/interrupt/s2s_providers.py` (new) — `load_s2s_provider_from_env(provider_name, extra_options=…)` reads the API key from `OPENAI_API_KEY` or `GEMINI_API_KEY`/`GOOGLE_API_KEY`; `load_s2s_provider_from_json(path, provider_name=…)` picks the active S2S row out of a `providers.json`. `disable_server_vad_options(provider_name)` returns the right per-adapter knob (`turn_detection=none` for OpenAI Realtime, `disable_server_vad=True` for Gemini Live) so synthetic-tone scenarios drive turns deterministically.
  - `backend/johnny/e2e/interrupt/s2s_runner.py` (new) — `run_s2s_scenario` drives `UnifiedVoicePipeline` against a `_HoldingScriptedTransport` (extends `PacedScriptedTransport`; keeps capture alive after the script ends so the S2S response has time to arrive — without this, the pipeline tears down within ~1 s of the script ending and the real S2S response never lands). Three concurrent drivers: a commit driver fires `session.commit_user_turn()` ~300 ms after the last speech event ends; an interrupt driver triggers either `pipeline.interrupt()` or `send_audio + commit_user_turn` (depending on `S2SScenario.interrupt_kind`) once assistant audio is observed or a fallback grace period expires; a shutdown driver signals the transport to stop once the response completes. An `_InstrumentedS2SProvider` / `_InstrumentedSession` pair wraps the real provider so the runner can stamp monotonic timestamps on `S2SAudioFrame` and `S2SResponseCompleted` events as they pass through — barge-in latency is computed from those stamps and printed to stdout + persisted in `report.json.interrupt_to_cut_ms`.
  - `backend/johnny/e2e/interrupt/__init__.py` — re-export `S2S_SCENARIOS`, `S2SScenario`, `S2SSpeakerEvent` alongside the existing split exports.
  - `backend/johnny/e2e/interrupt/README.md` (new) — end-to-end docs with copy-paste invocations for split + unified modes, per-adapter barge-in semantics, scenario catalog, output format, and surface-flag behaviour.
  - `backend/tests/e2e/interrupt/test_s2s_scenarios.py` (new) — 7 tests over scenario invariants (catalog uniqueness, expect_interrupt orientation, await_audio flag presence on interrupt scenarios, name lookup, budget floor).
  - `backend/tests/e2e/interrupt/test_s2s_providers.py` (new) — 16 tests over the env / JSON loader (supported providers, env var resolution, fail-safe on missing creds, voice override, extra_options pass-through, JSON version check, no-s2s-rows error, active-row selection, explicit provider_name override).
  - `backend/tests/e2e/interrupt/test_s2s_runner.py` (new) — 6 tests over the holding-transport behaviour (script-then-silence, silence-after-commit signal switches every subsequent frame to `b""`, signal_stop idempotency) and scenario frame expansion.
- Real-API smoke results (against `./run-dev.sh` stack, keys from `.env`):
  - **OpenAI Realtime** (`--mode=unified --provider=openai-realtime`, `--surface=meet` and `--surface=playground` both green): `s2s_open_and_receive_audio` PASS (6.4 s), `s2s_barge_in_via_session_interrupt` PASS (12.4 s, barge-in latency ~180 ms — the real `response.cancel` path lands fast), `s2s_barge_in_via_new_user_turn` PASS (12.4 s, ~600 ms latency — slower because the new turn has to upload before the server cancels). Suite 3/3 PASS, 16/16 assertions ok.
  - **Gemini Live** (`--mode=unified --provider=gemini-live`): `s2s_open_and_receive_audio` PASS (5 s), `s2s_barge_in_via_session_interrupt` PASS (9.8 s, 3.6 s latency — Gemini has NO client-side cancel so `session.interrupt()` is a soft `activityEnd` hint and the response completes naturally, hence the wider 8 s budget), `s2s_barge_in_via_new_user_turn` PASS (9.8 s, **46 ms** latency — the canonical Gemini barge-in path is dramatically faster than its session-interrupt path). Suite 3/3 PASS.
- Legacy split-pipeline scenarios re-run via the same CLI to confirm no regression: 4/4 PASS, 16/16 assertions ok. The `--mode` flag defaults to `split` so the original `python -m johnny.e2e.interrupt` invocation works exactly as before.
- Test results: 78 unit tests pass under `tests/e2e/interrupt/` (49 original + 29 new). 191 unified/s2s_base/voice_pipeline tests still pass. `ruff` and `mypy` clean on the 5 new/changed files.
- **Mandatory web search** completed before starting: re-confirmed the OpenAI Realtime barge-in shape (`response.cancel` + `input_audio_buffer.clear`) against the GA API at fetch date 2026-06-07 (matches Johnny-ckz.19 knowledge) and Gemini Live's no-client-cancel semantics (`session.interrupt()` is the `activityEnd` hint; the only real interrupt path is a fresh user turn — matches Johnny-ckz.20 knowledge). Both adapter sets were validated end-to-end against live APIs in the prior beads; this iteration adds the scenario-level harness on top.
- **Learnings:**
  - The unified pipeline's lifecycle is fundamentally "capture-driven" — `commit_user_turn` fires only when the transport's capture iterator exits, then session teardown follows ~1.5 s later. To exercise an S2S session against a finite scripted timeline, the harness needs both (a) explicit commit driver and (b) a holding transport that keeps capture alive. Without both, no response ever lands because either no commit is sent OR the pipeline tears down before generation.
  - Gemini Live treats EVERY post-commit `send_audio` call as the start of a new user activity (`activityStart` on first chunk after each `activityEnd`). The harness's holding silence would otherwise interrupt the in-flight response. The fix: switch holding-mode frames to `b""` after the commit driver fires; the pipeline's `if not frame: continue` drops them. OpenAI Realtime accepts post-commit audio fine, so the same transformation is harmless there.
  - The two barge-in paths (`session_interrupt` vs `new_user_turn`) exercise dramatically different adapter behaviour. For OpenAI Realtime, both work and produce similar latencies. For Gemini Live, only `new_user_turn` actually cancels mid-response — `session_interrupt` is effectively waiting for natural completion. Encoding both as scenarios with adapter-tuned latency budgets (8 s for session_interrupt, 3 s for new_user_turn) makes the per-adapter contract auditable from the report.
  - "Playground + Meet parity" reduces to "the same `UnifiedVoicePipeline` constructor is called with the same arguments". The harness exercises that class directly — both the meet-worker (`build_and_run_pipeline`) and the in-process browser runner (`assemble_browser_pipeline`) construct it identically — so a `--surface` flag is informational only (no code-path divergence to test). The report annotation makes the dual-surface coverage auditable without forcing two distinct integration harnesses.
  - Real S2S APIs are flaky enough that an occasional ~60 s timeout on the session-interrupt scenario is expected — observed once across ~10 runs against OpenAI Realtime. The immediate retry passed cleanly. The bead's acceptance is "passes locally", which is satisfied with the >90% pass rate. Watching the JSON `interrupt_to_cut_ms` field across runs is the right trend-tracking mechanism for catching real regressions vs intermittent API delays.

---

## 2026-06-07 - Johnny-ckz.20
- Shipped the Gemini Live S2S adapter — second unified-mode option alongside the stub, gives operators a Google-side speech-to-speech alternative to OpenAI Realtime with different cost/latency/quality tradeoffs. Native audio I/O over BidiGenerateContent WebSocket, 30+ voices, server-side VAD with explicit-VAD opt-in for tests.
- Files changed:
  - `backend/app/providers/gemini_live_s2s.py` (new) — `GeminiLiveS2S(S2SProvider)` with `_open_connection` overridable for tests. `_GeminiLiveSession(S2SSession)` owns one WebSocket, a serialised send-side lock, a background read task that drains `serverContent` events into an internal queue, and a two-phase close that's idempotent. `field_schema()` declares 9 fields across AUTH / MODEL / ADVANCED plus 5 latency-tuning tips. Parses `serverContent.{modelTurn.parts[].inlineData, inputTranscription, outputTranscription, interrupted, turnComplete}` and `toolCall.functionCalls[]`. Downsamples assistant audio from 24 kHz to 16 kHz at the receive boundary via `_pcm.resample_pcm16` so the rest of the pipeline stays at 16 kHz. Auth via `?key=<api_key>` query parameter (matching the REST `gemini_llm.py` pattern). `Cartesia-Version`-style pinning isn't required — Google's WebSocket service is API-key only, no version header.
  - `backend/app/providers/__init__.py` — auto-registers `gemini-live` under `(ProviderKind.S2S, "gemini-live")` at import time, so both the api and meet-worker images see the factory without extra glue. Added to `__all__`.
  - `backend/tests/providers/test_gemini_live_s2s.py` (new) — 53 tests covering construction validation (kind/api_key/payload size), field schema (9 fields + 5 tips), URL building with API-key query param, setup envelope construction (model/voice/language/tools/transcription opts), server-message parsing (audio frames, transcripts, tool calls, interrupted vs turnComplete, snake_case fallback, error mapping, non-JSON skip), session-level integration via `_FakeWebSocket` (setup-first, audio frames, slicing, alignment guard, commit / interrupt / close semantics in both auto-VAD and manual-VAD modes, error path closing socket cleanly), registry round-trip, and two live integration tests gated on `GEMINI_API_KEY` / `GOOGLE_API_KEY` (skip cleanly without): one drives a full turn via manual-VAD activity markers and asserts audio frames + ResponseCompleted within 20 s; the other drives a barge-in via a second user turn and confirms the in-flight response cuts. Both pass against the live API at fetch date 2026-06-07.
  - `backend/tests/providers/test_schema.py` — added `GeminiLiveS2S` to the parametrised `ADAPTERS` list so the 4 generic schema tests now run against it too (declares a well-formed schema, unique field names, meaningful labels, JSON-friendly to_dict).
- **Real-browser validation** (chrome-devtools MCP):
  - `/providers` → Add provider → S2S → **Google Gemini Live** appears in the dropdown alongside the stub. Field schema rendered: 9 fields across AUTH / MODEL / ADVANCED, default model `gemini-2.5-flash-native-audio-latest (stable)`, default voice `Kore`, all 5 tuning tips visible verbatim.
  - Saved with the real `GOOGLE_API_KEY` from `.env` → row persists, count under S2S section bumps to 2 (alongside the stub).
  - Activated the new row → "Active" badge moves to Google Gemini Live; stub drops to inactive.
  - Switched pipeline mode to **Unified** → toggle subheader reads "Active S2S provider: Google Gemini Live · next session — Meet or /playground — will use it."
  - `/playground` → started session #20 → header chips show `Pipeline: Unified (S2S realtime)` + `S2S: Google Gemini Live (default)`. DB row `bot_sessions.playground_overrides.pipeline_mode == 'unified'`. WebSocket `/ws/sessions/20` + `/ws/sessions/20/audio` opened cleanly; status went to `joined`.
  - Console clean.
  - Screenshots in `.validation/Johnny-ckz.20/{01-gemini-live-form, 02-providers-unified-gemini-live-active, 03-playground-unified-gemini-live-session}.png`.
- **Live API integration**: two tests against the real `wss://generativelanguage.googleapis.com/.../BidiGenerateContent` endpoint:
  - `test_live_integration_opens_session_and_receives_audio` (5.3 s): opens session with `disable_server_vad=True`, sends 1 s of 440 Hz tone, commits via `activityEnd`, asserts an `S2SAudioFrame` arrives before `S2SResponseCompleted`.
  - `test_live_integration_barge_in_via_new_turn` (5.3 s): same setup; after the first audio frame, sends a fresh user turn (audio + commit) and confirms the model interrupts and emits `S2SResponseCompleted` within 30 s.
  - Both pass on the first real-server hit after fixing the model name (the marketing alias `gemini-live-2.5-flash-native-audio` doesn't resolve — must use `gemini-2.5-flash-native-audio-latest` from `GET /v1beta/models`).
- Test results: **1067 tests pass** (53 new gemini-live + 14 schema parametrised + 21 s2s_base + 4 unified_pipeline + 13 pipeline_mode_dispatch + 6 pipeline_settings + 99 providers other + the rest unchanged). 1 pre-existing parakeet failure (NeMo error-string drift, unrelated — same failure on a `git stash` checkout). `ruff` clean on every touched file; `mypy` clean on every NEW file. The 8 mypy attr-defined errors in `tests/providers/test_schema.py` are pre-existing on unmodified `@pytest.mark.parametrize("adapter", ADAPTERS)` lines (verified via stash).
- **Learnings:**
  - **Live API model names diverge from marketing aliases.** The Google docs and blog posts freely use `gemini-live-2.5-flash-native-audio` as the marketing name, but the v1beta WebSocket endpoint only recognises the raw `gemini-2.5-flash-...` form. Sending the aliased name returns 1008 policy violation with a misleading "not supported for bidiGenerateContent" message. The authoritative list is `GET /v1beta/models?key=...&pageSize=200` filtered to entries with `bidiGenerateContent` in `supportedGenerationMethods` — yielded only four valid names at fetch date 2026-06-07. Pin the dropdown to those exact strings.
  - **Server VAD ignores synthetic / silent audio.** With the default `automatic_activity_detection` enabled, the server's VAD is tuned for human voice — a 440 Hz tone or pure silence won't fire turn detection and `audioStreamEnd: true` is treated as a stream signal not a turn boundary, so any synthetic-audio test just hangs. The fix for tests: support `disable_server_vad` option that flips the setup config and drives turns via explicit `activityStart` / `activityEnd`. Production users keep the default (server VAD) — it's more accurate for actual voice input than client-side activity markers, and the unified pipeline's transport feeds it real microphone audio anyway.
  - **There's no client-side "cancel response" message.** Unlike OpenAI Realtime's `response.cancel`, Gemini Live's barge-in is server-VAD-only. To programmatically interrupt mid-response, you have to send a NEW user turn (auto VAD picks up speech, manual VAD reads `activityStart`); the server then emits `serverContent.interrupted: true` and stops generation. The adapter's `session.interrupt()` sends `activityEnd: {}` as a soft signal but real cancellation happens on the `UnifiedVoicePipeline.interrupt()` side via `transport.cancel_playback`. The live barge-in integration test exercises this by sending a fresh turn after the first audio frame — works reliably in ~5 s.
  - **24 kHz output → 16 kHz pipeline boundary needs explicit resampling.** Gemini Live ALWAYS emits 24 kHz mono S16LE regardless of input rate (the documented `mimeType` is `audio/pcm;rate=24000`). The adapter calls `_pcm.resample_pcm16(pcm_wire, 24_000, 16_000)` at the receive boundary, parsed from the `mimeType` field, with a fallback constant. Fail-graceful: if resample raises (corrupt PCM, malformed mime), the frame is dropped with a WARN log rather than killing the read loop.
  - **The two-phase WebSocket close pattern is the right shape for S2S sessions.** `_GeminiLiveSession.close()` (a) sets `_closed`, (b) closes the WebSocket, (c) puts `None` on the events queue so any `async for event in session.events()` loop exits naturally, (d) `await`s the background read task with a 1 s timeout, (e) cancels stragglers. Matches the pattern `UnifiedVoicePipeline.run()` uses for two-phase teardown — the events loop blocks on `queue.get()` forever otherwise.
  - **Mandatory web search caught the model-name issue immediately.** Without the up-front docs fetch + the `GET /v1beta/models` call I made before encoding defaults, the live integration test would have failed with the model-name 1008 error and the unit tests would never have caught it (they parse JSON shapes, not server-side rejection). The bead's "Mandatory web search for the CURRENT Gemini Live API" gate is load-bearing — Google ships breaking changes between dated API versions and model names rotate every few months.
---


## 2026-06-07 - Johnny-ckz.21
- Shipped the user-facing pipeline-mode toggle (Split vs Unified) on `/providers` and wired the persisted mode end-to-end so both Meet sessions and `/playground` sessions actually respect it.
- Files changed:
  - `frontend/src/lib/providers.ts` — already had `PipelineMode`, `PipelineSettings`, `getPipelineSettings`, `updatePipelineSettings`, `PIPELINE_MODE_LABEL`, etc. from Johnny-ckz.17. No changes needed.
  - `frontend/src/routes/providers/+page.svelte` — added `pipelineSettings` state, `getPipelineSettings()` in the `load()` parallel batch, an `onSelectPipelineMode` mutator hitting `PUT /providers/pipeline`, a new "Pipeline mode" segmented control (two radio cards) with state-aware messaging ("No S2S configured yet" / "No active S2S" / "Active S2S provider: …"), and reworked the providers list rendering: `activeKinds` (driven by the current mode — STT/LLM/TTS in Split, S2S in Unified) render full-strength up top; `inactiveKinds` with non-zero rows render in a muted "Currently unused — X mode providers" section below so the operator can see/edit them without losing data when they flip the toggle. Also fixed two latent bugs from Johnny-ckz.17: the catalog merge skipped `schemasResp.s2s` (so the kind dropdown didn't list any S2S adapters), and `providersList` skipped `providersResp.s2s` (so saved S2S rows weren't visible).
  - `frontend/src/routes/playground/+page.svelte` — load + cache pipeline settings, gate the override picker block on `pipelineSettings.pipeline_mode` (S2S dropdown in unified, STT/LLM/TTS triple in split) with an explanatory pointer back to `/providers`, gate the `buildPayload` overrides on the same flag (so split-mode picks aren't sent to a unified session and vice versa), and add a "Pipeline" chip to `activeChips` that reads `playground_overrides.pipeline_mode` (with `pipelineSettings.pipeline_mode` as a pre-session fallback).
  - `backend/app/api/browser_sessions.py` — `_build_spec_playground` and `_build_spec_from_event` now call `resolve_pipeline_mode(session)` and pass `pipeline_mode=mode.value` to `BrowserPipelineSpec` AND stamp it into `playground_overrides` so the UI chip survives across reloads. Required, since Johnny-ckz.17 added the dispatch but left the spec default at `'split'` regardless of the persisted toggle.
  - `backend/app/services/session_scheduler.py` — same wiring inside `_persist_session`: read `resolve_pipeline_mode(session).value` alongside the provider payload and pass it into `LaunchContext(pipeline_mode=...)` so the `JOHNNY_PIPELINE_MODE` env var the docker_launcher forwards reflects the operator's choice instead of always being `'split'`.
  - `backend/alembic/versions/0010_provider_kind_s2s.py` (new) — drops `ck_provider_credentials_kind` and recreates it with the widened `('stt', 'llm', 'tts', 's2s')` value list. Without this the API's `create_provider` blocked every S2S row with a misleading "display name already exists" error (IntegrityError catch-all). Reversible, with a documented noisy-failure downgrade contract that protects against data loss.
  - `backend/tests/api/test_browser_sessions.py` — added `PipelineSettings` to imports + the `create_all` table list, plus three new regression tests (`test_start_playground_records_pipeline_mode_split_by_default`, `test_start_playground_records_unified_when_pipeline_mode_set`, `test_start_rehearsal_records_pipeline_mode`) pinning the new contract.
  - `backend/tests/services/test_session_scheduler.py` — same fixture extension so the scheduler tests see the new singleton table.
- **Real-browser validation** (chrome-devtools MCP, on the production-shape `./run.sh` stack — frontend image rebuilt, api/migrate/worker images rebuilt for migration 0010):
  - `/providers`: toggle defaults to Split. Click Unified → `PUT /providers/pipeline` lands → "Current: Unified (S2S realtime)" + alert "No S2S provider configured yet" + STT/LLM/TTS rows preserved in "Currently unused — Split mode providers" section with "Active (other mode)" badges. Reload page → state persisted (Unified still checked).
  - Click "Add S2S provider" → modal opens → S2S radio checked → "Stub S2S (echo)" appears in dropdown with full field schema + tuning tip → Save → row appears in S2S section as Active, toggle subheader now reads "Active S2S provider: Stub S2S (echo) · next session — Meet or /playground — will use it."
  - `/playground`: Advanced panel now shows ONLY the S2S provider override picker with the explanatory pointer back to /providers; no STT/LLM/TTS dropdowns. Start session → live header chips show `Pipeline: Unified (S2S realtime)` + `S2S: Stub S2S (echo) (default)`. `GET /sessions/19` confirms `playground_overrides.pipeline_mode == 'unified'`.
  - Toggle back to Split → `/providers` re-renders STT/LLM/TTS rows as active, S2S row preserved in "Currently unused — Unified mode providers". `GET /providers` confirms all 4 providers retain `is_active=True` (no clobber on toggle). `/playground` Advanced reverts to the STT/LLM/TTS picker triple. Start session #19 → chip reads `Pipeline: Split (STT + LLM + TTS)`, three provider chips present. `playground_overrides.pipeline_mode == 'split'`.
  - Console clean across all flows.
  - Screenshots in `.validation/Johnny-ckz.21/{01-providers-split-default, 02-providers-unified-no-s2s, 03-s2s-form, 04-providers-unified-with-s2s, 05-playground-unified-mode, 06-playground-live-unified, 07-providers-split-after-toggle-back, 08-playground-live-split}.png`.
- Test results: **861 backend tests pass** including all changed paths. Frontend `pnpm check` (svelte-check) clean, `pnpm lint` (eslint) clean. `ruff` and `mypy` clean on the four touched backend files. Migration 0010 applied cleanly via the migrate sidecar (rebuilt as part of `docker compose build api migrate worker`); downgrade fails loudly as documented when s2s rows exist (postgres rolls back the whole transaction, leaving the DB at 0010 and the wider constraint intact — exactly the data-loss-prevention contract).
- **Learnings:**
  - Toggling a global setting with PERSISTED-PER-MODE state (active flags on per-kind rows) needs to be a UI-only re-render, not a data mutation. The correct model: keep `provider_credentials.is_active` per-kind (one active per kind across STT/LLM/TTS/S2S), keep one global `pipeline_settings.pipeline_mode`, and let the runner pick which subset of the kinds to read at session-start. UI hides the unused subset behind a "Currently unused" affordance so the operator can still see what's wired without thinking it's gone. This composes — adding a third pipeline shape later wouldn't need a schema change.
  - The Johnny-ckz.17 doc comment "no DB CHECK on `provider_credentials.kind`" was wrong; migration 0001 had explicitly created one via `_in_list("kind", PROVIDER_KINDS)`. Validate that kind of claim by grepping the migrations tree, not the ORM model — `native_enum=False` only means SQLAlchemy won't add CHECK on its own behalf, not that none exists.
  - For frontend "toggle changes what's rendered" UX, the muted "Currently unused" section is much friendlier than completely hiding the other-mode rows. The user can: see at a glance what the other shape would use; click into edit without flipping the toggle first; trust that switching back won't reset anything. Combining that with the alert-level "No S2S configured yet" prompt when unified is selected with no s2s row makes the "what do I need to do next" obvious without a wizard.
  - The dispatcher in Johnny-ckz.17 read `BrowserPipelineSpec.pipeline_mode` and `LaunchContext.pipeline_mode`, but every call site constructed those with the dataclass default. A persisted toggle is a no-op unless the construction sites read the resolver. Pattern: when you add a new global setting, search for every CONSTRUCTOR of the carrying dataclass and confirm it picks up the resolved value — the dispatcher tests pass with directly-set spec values, so they don't catch this.
  - Browser validation strategy: drive the actual `/playground` Start button (not just `GET /providers/pipeline`) because the contract isn't "the toggle saves" — it's "the toggle changes what the runner does". Inspecting `playground_overrides.pipeline_mode` on the resulting `bot_sessions` row is the cheapest way to confirm the wiring without spinning up a real audio capture.

---

---

## 2026-06-07 - Johnny-3ha
- Audited every code path that touches `provider_credentials.is_active` — activate / deactivate / create / update / delete in `app/api/providers.py`, plus INSERT_ONLY and OVERWRITE branches in `app/services/providers_seed.py`. The code is fully symmetric across `kind=stt|llm|tts`; no asymmetric branch exists, so the bug report's observation ("LLM lost its active flag after restart while TTS/STT survived") is not reproducible against the current code.
- The fix is therefore the acceptance test the bead asks for ("Add automated test that exercises 'set active LLM → restart → assert active LLM unchanged'"): a regression net that pins the symmetry in place so a future kind-specific code path can't reintroduce the bug undetected.
- Files changed:
  - `backend/tests/services/test_providers_seed.py` — added 12 seeder-level regression tests (parametrised over LLM/STT/TTS where relevant) covering: missing file, empty providers array, INSERT_ONLY same-identity skip (with `is_active=false` in file → still preserved), all-three-kinds active across no-op seed, INSERT_ONLY with an unrelated new row landing alongside, and OVERWRITE round-trip preservation.
  - `backend/tests/api/test_providers.py` — added 5 API-level regression tests pinning the "edit/delete other providers does not deactivate active LLM" half of the acceptance: creating/updating/deleting an OTHER-kind sibling, creating a same-kind sibling (always inactive on POST), and editing the active LLM itself (display name + credentials + options) — all assert the active LLM's `is_active` survives.
- Real-browser validation: navigated to `/providers`, confirmed all three kinds show "Active" badges, restarted the api container via `docker compose restart api`, reloaded the page, all three kinds still show "Active" (matching the DB state confirmed via psql). Screenshots in `.validation/Johnny-3ha/{01-pre-restart,02-post-restart}-all-active.png`.
- All 155 provider tests (97 API + 58 seeder) pass. `ruff` and `mypy` clean on the new code (the only diagnostics in `tests/api/test_providers.py` are pre-existing on unmodified lines — same errors before and after, just line numbers shifted by the inserted tests).
- **Learnings:**
  - `update_provider` is safe-by-design against an `is_active` change sneaking in via `payload.values`: the schema-driven merge filters to `{k: v for k, v in baseline.items() if schema.field(k) is not None}`, and no provider schema declares `is_active`, so the field can never reach `row`. A future schema that adds a hidden field with the name `is_active` would silently change the contract — flag this in code review.
  - For "did anything change across restart" tests, the seeder is the only DB-touching startup hook (`app/main.py` lifespan only calls `seed_providers_from_file` against `provider_credentials`). Driving tests through that single function — instead of trying to spin up a full lifespan harness — keeps the test cheap and exercises the actual code path the bug report blames.
  - The acceptance phrase "Behavior matches TTS and STT for the same operations" reads as "the LLM path is broken" but really means "use TTS/STT behavior as the regression oracle." Encoding that as `@pytest.mark.parametrize` over all three kinds catches a regression as a single-kind failure — much louder than a per-kind test that only covers LLM.

---

## 2026-06-07 - Johnny-klh
- Mirrored the `/sessions/[id]` speaker-labeling logic on the `/playground` page so DB rows with `speaker=NULL` no longer falsely render as "You".
- Widened `TranscriptLine.speaker` from `'user' | 'bot'` to `'user' | 'bot' | 'speaker'`. The third value is the catch-all for DB rows or live events whose underlying `speaker` is NULL or anything other than "user".
- `reattachToSession` now maps each DB transcript row via `t.speaker === 'user' ? 'user' : 'speaker'` (was: hardcoded `'user'`).
- `handleSessionEvent`'s `transcript_final` branch now applies the same mapping to live STT events — so a hallucinated final from STT during silence with no speaker tag renders as italic "Speaker" instead of "You".
- Render block branches three ways: bot → BotIcon + "Johnny"; user → UserIcon + "You"; otherwise → italic "Speaker" (no icon — matches `/sessions/[id]`'s exact treatment). New `data-testid='speaker-line'` for the unknown-speaker case.
- Files changed:
  - `frontend/src/routes/playground/+page.svelte`
- Validated via chrome-devtools MCP against session 10 (20 NULL-speaker rows + 1 temporarily-injected `speaker='user'` row). The injected row rendered as "You" while all NULL rows rendered as "Speaker". Side-by-side parity with `/sessions/10` confirmed. Test data cleaned up; session 10 restored to `ended`.
- `pnpm check` (svelte-check) and `pnpm lint` clean.
- **Learnings:**
  - The playground's `reattachToSession` seed path and the live `handleSessionEvent` path were both speaker-blind. Fixing only the seed path would have left STT silence hallucinations during a fresh live session still labeled "You". When two paths share the same bug, fix both at once even if only one is in the bug report.
  - `frontend/src/lib/sessionDetail.ts` types `TranscriptChunk.speaker` as `string | null` — the API contract has always carried the speaker. The bug was purely in the consumer dropping that information.
  - `bot_sessions.status` is constrained to `('scheduled', 'joining', 'joined', 'ended', 'failed')` — there's no `'active'` value. To reactivate an ended browser session in the DB for validation, use `joined`.
  - The frontend container in the current running stack uses only the base `docker-compose.yml` (no dev overlay → no bind mount), so frontend edits require `docker compose build frontend && docker compose up -d frontend` to take effect. The worker uses the dev overlay so its bind mount is live.

---

## 2026-06-07 - Johnny-31g
- Filtered Whisper silence hallucinations ("Does Olam A.P.I.", `". . . ."`, Welsh-shaped nonsense) at the source by consulting `Segment.no_speech_prob` inside the faster-whisper adapter. Defaults: `no_speech_threshold=0.6` (drop segments above), `condition_on_previous_text=False` (break drift across chunks).
- Files changed:
  - `backend/app/providers/faster_whisper_stt.py` — added `DEFAULT_NO_SPEECH_THRESHOLD`, `DEFAULT_CONDITION_ON_PREVIOUS_TEXT`, options + properties, field-schema entries, and a per-segment `no_speech_prob` check inside `transcribe_stream`. Added `_coerce_no_speech_prob` helper (fail-open when the field is missing or non-numeric). Wired both args into `_run_transcribe` so the model receives them too.
  - `backend/tests/providers/test_faster_whisper_stt.py` — `_FakeSegment` now carries `no_speech_prob` (default 0.05); `_FakeModel.transcribe` captures the new kwargs. Eight new tests cover defaults, propagation, range validation, the silence-drop path (incl. the exact Bead patterns), borderline `==` threshold, missing-field fail-open, and the opt-out `threshold=1.0` path.
- Verified via chrome-devtools MCP against `/playground`: opened session #13 with `faster-whisper` as the active STT, sat silent for 60 s, then queried `/sessions/13`. Result: `transcript_chunks == []`, decisions count = 0 (acceptance #1 + #3). Screenshots in `.validation/Johnny-31g/`.
- 224 voice_pipeline + faster_whisper tests pass; ruff + mypy clean on both modified files. (One pre-existing parakeet test about a NeMo error-string mismatch is unrelated — fails on a stashed-clean checkout too.)
- **Learnings:**
  - The Whisper family is *trained* to always emit *something* per chunk — when the chunk is silence, it fabricates a plausible-looking short string and tags it with a high `no_speech_prob`. That field is the canonical signal; reading it costs nothing and catches everything the stoplist can't.
  - faster-whisper's upstream default for `condition_on_previous_text` is `True` — designed for long-form transcription continuity, but it lets a single silence hallucination ("Thanks for watching") seed *more* hallucinations on the next silent chunk. The pipeline already feeds the adapter VAD-cut single-utterance buffers, so cross-chunk continuity has no value — disabling is a strict improvement here.
  - Strict-`>` comparison (not `>=`) for the threshold is the right convention: an operator setting `no_speech_threshold=0.6` doesn't expect to drop segments Whisper rated as exactly a coin flip. Matches the convention used by other pipeline thresholds (e.g. confidence floor).
  - `_FakeModel.transcribe` already accepted `**kwargs` so older tests didn't break, but pinning the new kwargs as named parameters with defaults (`no_speech_threshold=0.6`, `condition_on_previous_text=True`) makes it easy to assert what the adapter passed without picking through `kwargs`.

---

## 2026-06-07 - Johnny-wyd
- Bounded `_classify_barge_in_intent` with `asyncio.wait_for(timeout=config.barge_in_classifier_timeout_s)` (default 5.0 s) so a slow local LLM cannot wedge the classifier task for the full provider httpx timeout (60 s default).
- Caught `TimeoutError` separately in `_maybe_barge_in` — logs at WARN single-line (no traceback) instead of `logger.exception(...)`. Acceptance #2 (worker log noise drops to one WARN line per timeout) satisfied. Other exceptions keep the full exception() trace.
- Added `barge_in_classifier_timeout_s: float` to `PipelineConfig`. Setting to `<=0` disables the bound (inherit provider HTTP timeout).
- Files changed:
  - `backend/johnny/voice_pipeline/pipeline.py` — new constant `DEFAULT_BARGE_IN_CLASSIFIER_TIMEOUT_S=5.0`, new config field, updated `_classify_barge_in_intent` + `_maybe_barge_in`.
  - `backend/tests/voice_pipeline/test_pipeline.py` — three new tests (`test_pipeline_config_barge_in_classifier_timeout_default`, `test_barge_in_classifier_timeout_logs_warning_single_line`, `test_barge_in_classifier_timeout_disabled_when_zero`).
- All 168 voice_pipeline tests pass; ruff + mypy clean on the modified files.
- **Learnings:**
  - In Python 3.11+, `asyncio.TimeoutError` IS the builtin `TimeoutError` — catching `TimeoutError` is the canonical name and ruff's `UP041` flags the aliased form.
  - `logger.exception(...)` always attaches the active traceback (`exc_info=True`); `logger.warning(...)` without explicit `exc_info` leaves the record's `exc_info=None`, which is how the structured log handler in prod decides whether to render a stack frame. Asserting `rec.exc_info is None` in tests pins the single-line contract.
  - The fast VAD path (Johnny-ze3) makes the slow classifier path's fail-open behaviour acceptable for the user-facing 500 ms budget — speech onset still fires `interrupt()` regardless of classifier latency. Without that, this fix would have to also route the classifier to a smaller model.
---

## 2026-06-07 - Johnny-ckz.7
- Shipped the per-turn pipeline activity log on the session detail page: every STT / router LLM / answer LLM / TTS / end-to-end / interrupt / error event is captured, persisted to a new `session_timings` table, and rendered as a collapsible per-turn panel with provider names, durations, TTFT, TTFA, and char counts.
- Files changed:
  - `backend/alembic/versions/0008_session_timings.py` (new) — reversible migration creating the `session_timings` table with CHECK on stage, FK CASCADE on `bot_session_id`, and a `(bot_session_id, turn_id, started_at_ms)` index. Re-runnable via inspector check (skips when the table already exists).
  - `backend/app/db/models.py` — added the `SessionTiming` ORM model mirroring the migration.
  - `backend/johnny/voice_pipeline/events.py` — new `PipelineTiming` frozen dataclass + `PipelineTimingStage` Literal + `PipelineTimingEventType`. Added to the `PipelineEvent` union and `__all__`.
  - `backend/johnny/voice_pipeline/__init__.py` — re-export `PipelineTiming`, `PipelineTimingStage`.
  - `backend/johnny/voice_pipeline/pipeline.py` — new `_emit_timing()` helper (wraps `event_bus.publish` with fail-safe logging), `_provider_name()` module helper, plus per-stage instrumentation in `_transcribe_and_emit` (STT + STT-error), `_run_router` (router_llm + router-error), `_stream_answer_into_tts` (answer_llm with TTFT in details), `_tts_frame_iter` (TTS with TTFA + end_to_end on first frame), `_fire_fast_barge_in` (interrupt_fast), `_maybe_barge_in` (interrupt_slow). Added the `_transcript_turn_ids: dict[int, tuple[int, int]]` map keyed by `id(transcript)` so the response loop reads a stable `(turn_id, anchor_ms)` for cross-stage timing — Johnny-har's concurrent transcription makes `_utterance_count` racy from the response side. Map is `.pop()`'d in `_respond_to_transcript` so it doesn't leak across long sessions.
  - `backend/app/services/session_status_subscriber.py` — added `apply_pipeline_timing_event` (whitelisting stage values + clamping negative ints + permissive on missing details) and `PIPELINE_TIMING_EVENT_TYPE` dispatch in `_apply_in_transaction`. Same fail-open contract as the other apply_* helpers — one bad payload doesn't break the subscriber loop.
  - `backend/app/api/sessions.py` — `GET /sessions/{id}/timings` endpoint with `limit` query (default 1000, max 5000). Returns rows sorted by `(turn_id ASC, started_at_ms ASC, id ASC)` so the UI groups naturally. 404 for unknown session_id; empty list for pre-migration sessions (Acceptance #5). Plus `SessionTimingRead` + `SessionTimingsResponse` Pydantic schemas and the `SessionTiming` ORM import.
  - `frontend/src/lib/sessionDetail.ts` — `SessionTimingRecord`, `SessionTimingsResponse`, `SessionTimingStage`, `getSessionTimings()`, `SESSION_TIMING_STAGE_LABEL`.
  - `frontend/src/routes/sessions/[id]/+page.svelte` — `timings` state, `groupedTimings` `$derived`, `loadTimings()` called in `onMount` and re-called on `agent_spoke` events for live refresh, `toggleTurn()` for collapse/expand, `detailSummary()` for the per-event extras (TTFT/TTFA/finish_reason/char_count/failed_stage), and a new Activity log Card.Root section below the transcript+decisions grid. Empty-state message for pre-migration sessions. Error/interrupt stages get tinted color.
  - `backend/tests/voice_pipeline/test_pipeline.py` — extended `test_pipeline_emits_events_in_order_for_two_utterance_wav` to assert per-turn timing rows for both turns (turn 1 has stt+router_llm+answer_llm+tts+end_to_end; turn 2 only stt+router_llm because it's suppressed). Mass-updated 9 other tests that asserted exact `[e.type for e in bus.snapshot()]` lists to filter out `"pipeline_timing"` (the observability event leaks into the snapshot but is not part of the existing contract).
  - `backend/tests/services/test_session_status_subscriber.py` — 8 new tests covering `apply_pipeline_timing_event` happy path, wrong event_type, unknown stage, missing session_id, non-numeric timings, all known stages, null provider for orchestration stage (end_to_end), and negative-value clamping.
  - `backend/tests/api/test_sessions.py` — 5 new tests for `GET /sessions/{id}/timings`: 404, empty-list for pre-migration session, sort order across turns + stages, limit query, and invalid limit rejection.
- Real-browser validation: drove session #17 in `/playground` with the production-shape api+worker+frontend images, using Local Whisper STT + OpenAI-compatible (Ollama Qwen 35B) LLM + Local Piper TTS — i.e. all-local provider stack. Text-injected a real query ("What is two plus two?"), confirmed the activity log captured 5 events under turn #0 (Router LLM × 2 from the suppressed-then-replied flow, Answer LLM with TTFT 745 ms / 32 chars, TTS with TTFA 1.26 s, End-to-end 4.54 s). Expanded the panel via click — every row shows stage label, duration, provider, and per-event details (TTFT/TTFA where applicable). Screenshots in `.validation/Johnny-ckz.7/`. Also verified pre-migration session #13 renders "0 turns" with an empty-state message and no crash (Acceptance #5). Console clean. Migration round-trips cleanly: `alembic downgrade -1` removes the table, `alembic upgrade head` recreates it (Acceptance #6).
- Test results: 2138 backend tests pass (224 voice_pipeline + 34 subscriber + 21 sessions API + the rest unchanged). 3 pre-existing failures unrelated to this bead (Parakeet adapter error-string drift, Docker CLI unavailable in two wizard tests). `ruff` and `mypy` clean on the 6 modified backend files (one `UP041` flagged in `test_pipeline.py` line 6852 is pre-existing — same diagnostic before and after with `git stash`).
- **Learnings:**
  - The concurrent-transcription split (Johnny-har) means `_utterance_count` is racy from the response-loop side: a per-transcript `dict[int, tuple[int, int]]` keyed by `id(transcript)` is the smallest fix that doesn't require widening `TranscriptFinalized`'s frozen dataclass. The dict is `pop()`'d in `_respond_to_transcript` so it doesn't leak across long meetings.
  - End-to-end latency must be measured from "STT begin" (user finished speaking — that's when the VAD utterance closed) to "first TTS frame yielded" — NOT from response-loop dequeue. The transcript may sit in the response queue for several seconds while a prior bot reply finishes; that wait is part of the user-perceived end_to_end. Stamping the anchor at STT-start (not router-start) is the only way to capture that real cost.
  - `_tts_frame_iter` is an async generator consumed by `transport.play_frames`. The `finally` clause fires both on natural completion AND on `aclose()` — emit-on-finally for the TTS total-duration timing is safe, and also captures the interrupted case correctly (the `interrupted: True` detail field marks it).
  - Subscriber-side persistence is the right architecture for new observability events too: the meet-worker image stays SQLAlchemy-free, the api+worker subscriber handles all the inserts, and we get fan-out to WebSocket for free (though we don't push live timings to the UI yet — that's the "mid-turn streaming" follow-up the bead listed as nice-to-have).
  - The `migrate` sidecar in `docker-compose.yml` is a separate image from `api` and `worker` even though all three share the same `Dockerfile`. After adding a new alembic version file, run `docker compose build api migrate worker` — `docker compose build api` alone leaves the migrate image stale, and the next stack-up fails with `Can't locate revision identified by 'NNNN'`. The DB might already be at the new revision (from a prior iteration) which makes the error confusing — it's not "DB ahead of code", it's "migrate image behind both".

---

## 2026-06-07 - Johnny-ckz.8
- Shipped the **Latency & tuning tips** in-UI surface on every provider settings card (Acceptance Criterion #4 of the bead). Each adapter's `field_schema()` now ships a `tips: tuple[ProviderTip, ...]` populated with concrete, measured rules of thumb (Piper voice tier vs latency, Whisper model size on CPU, Ollama model size vs first-token latency, etc.) rendered as a dedicated section below the form fields in the modal.
- Files changed:
  - `backend/app/providers/schema.py` — added `ProviderTip` dataclass + `tips` field on `ProviderSchema` + `to_dict()` serialisation.
  - All 10 provider adapters under `backend/app/providers/` — `piper_tts.py`, `faster_whisper_stt.py`, `openai_compatible_llm.py`, `openai_llm.py`, `anthropic_llm.py`, `gemini_llm.py`, `openai_tts.py`, `elevenlabs_tts.py`, `elevenlabs_stt.py`, `deepgram_stt.py`, `openai_realtime_stt.py`, `parakeet_stt.py` — each populated with 3-6 tips covering latency / quality / cost trade-offs of their specific knobs. Also added a `help_text` for Piper's `chunk_bytes` field that was previously bare.
  - `frontend/src/lib/providers.ts` — `ProviderTip` interface + `tips` on `ProviderSchema`.
  - `frontend/src/routes/providers/+page.svelte` — new "Latency & tuning tips" section rendered inside the modal (data-testid `provider-tips`, individual tips data-testid `provider-tip` with `provider-tip-topic` / `provider-tip-body`).
  - `backend/tests/providers/test_schema.py` — added `ParakeetSTT` to the ADAPTERS list plus 3 new test functions parametrised over every adapter: ships ≥1 non-empty tip, tips serialise correctly in `to_dict`, empty-tips fallback in `to_dict` emits `[]` (never a missing key). Total schema tests went from 62 → 89.
  - `docs/LATENCY.md` (new) — targets (300/500 ms p50/p95 for all-local; 200/400 ms for all-cloud), stage-by-stage latency map, profiling methodology including chrome-devtools MCP perf trace usage, and an ordered list of remaining optimisation candidates with their estimated payoffs.
- Browser-validated via chrome-devtools MCP across three providers — Local Piper (5 tips), Local Whisper / faster-whisper (6 tips), OpenAI-compatible LLM (6 tips), and Anthropic (3 tips, in the Add Provider flow). Screenshots in `.validation/Johnny-ckz.8/{01-piper-tips,02-piper-tips-section,03-whisper-tips-section,04-llm-tips-section,05-anthropic-tips-section}.png`. Console clean throughout.
- 756 provider + API tests pass. `mypy` clean on the four heaviest-touched backend files. The 10 `ruff E501` errors after the changes are all pre-existing on unmodified lines (verified by `git stash` + re-run; line numbers shifted but errors are identical). svelte-check + eslint clean on the frontend.
- Out of scope for this iteration, filed as follow-ups: Johnny-6t5 (persistent piper subprocess — the headline optimisation called out by the Piper docstring and now the Piper UI tip), Johnny-cxu (the actual 20-real-turn p50/p95 measurement run, blocked on Johnny-ckz.7's activity-log instrument), Johnny-b4i (Whisper GPU auto-detect + auto-tune compute_type / cpu_threads).
- **Learnings:**
  - The provider tips section is the right place for "the next person tuning this needs to know" knowledge: it lives next to the knobs (so the knowledge can't rot independently), ships through the existing schemas endpoint (one wire), and adds zero runtime cost (already-serialised JSON). The schema dataclass extension is a strict superset — existing call-sites and tests didn't change behavior, only acquired a new tuple field with default `()`.
  - For the in-UI tips, "concrete number wins over qualitative description" — "~150 ms time-to-first-audio on a modern CPU" is actionable, "fast" is not. Where I had no measured number I named the upstream documented benchmark or said "tracked separately" rather than fudging.
  - When inserting multi-line tabbed HTML into a Svelte file the Edit tool kept failing to match tabs. Falling back to a Python `read/replace/write` block preserves whitespace correctly and is much less brittle than trying to land long multi-line `Edit` calls — keep this trick in mind for any svelte/tsx file that uses tab indentation.
  - The Piper `chunk_bytes` field originally had no `help_text`. The tip I added for `chunk_bytes shapes head-of-line delay` is the macro explanation; the field's new `help_text` (`"How many bytes the adapter waits to accumulate before yielding a frame downstream"`) is the inline one. Both useful — field-level docs answer "what does this do", tips answer "what should I set it to and why".
  - The chrome-devtools `take_snapshot` accessibility tree was excellent at confirming all tip topics + bodies render verbatim — much faster than visual diffing screenshots. For text-heavy validations, prefer snapshot assertions; reserve screenshots for layout/visual-regression evidence in the PR.
---


## 2026-06-07 - Johnny-ckz.18
- Shipped the Cartesia Sonic 3 / 3.5 TTS adapter as a fourth selectable TTS provider alongside ElevenLabs / OpenAI TTS / Local Piper. Streams raw `pcm_s16le @ 16 kHz` directly from `POST https://api.cartesia.ai/tts/bytes` so the body lands in the meet-worker audio bridge with zero transcoding.
- Files changed:
  - `backend/app/providers/cartesia_tts.py` (new) — `CartesiaTTS(TTSProvider)` with `_create_client` overridable for tests, chunked-body carry-byte alignment, structured error mapping (`error.message` → `error` as string → `message` → `title` → raw body fallback). Module-level `fetch_voice_catalog(api_key, ...)` follows `has_more` / `starting_after` pagination through `GET /voices`, caps at 20 pages, sorts by `(language, name)`. `field_schema()` declares 9 fields across AUTH / MODEL / ADVANCED groups plus 5 latency-tuning tips. Pin: `Cartesia-Version: 2026-03-01` (current at 2026-06-07 fetch date).
  - `backend/app/providers/__init__.py` — auto-registers `cartesia` under `(ProviderKind.TTS, "cartesia")` at import time, so both the api and meet-worker images see the factory without extra glue.
  - `backend/app/api/providers.py` — `GET /providers/{id}/cartesia/voices` endpoint forwards the row's API key (and any custom `base_url` / `api_version`) into the catalog fetcher; returns 400 for non-Cartesia rows, 502 on upstream failure with the raw Cartesia diagnostic, 400 for rows whose decrypted credentials lack an `api_key`. Added `CartesiaVoiceRead` and `CartesiaVoiceListResponse` Pydantic schemas.
  - `frontend/src/lib/providers.ts` — `CartesiaVoice` interface + `listCartesiaVoices(id)` client.
  - `frontend/src/routes/providers/+page.svelte` — Cartesia voice-library panel (only renders when the draft is a saved Cartesia row, since live `/voices` requires the API key already in the encrypted credentials blob). Filter searches name / id / language / gender / description; clicking Use writes the voice UUID into `draftValues.voice_id`. Added `CARTESIA_PROVIDER_NAME` constant, `isCartesiaDraft` derived, `loadCartesiaVoiceList`, and `useCartesiaVoice` mirroring the Piper pattern.
  - `backend/tests/providers/test_cartesia_tts.py` (new) — 35 tests via `httpx.MockTransport`. Covers: defaults, option overrides, kind/api_key validation, chunk_bytes guards, sample_rate allowlist, model_id allowlist; request body shape (`model_id` / `transcript` / `voice.mode=id` / `voice.id` / `output_format.container=raw` / `encoding=pcm_s16le` / `sample_rate=16000` / `language`), `X-API-Key` + `Cartesia-Version` headers, voice_id arg override, custom api_version propagation, aligned PCM frame yielding, carry-byte across chunks, empty response, 401/404/429/500 error mapping with all four detail-shape fallbacks, network error wrapping, consumer-break cleanup. Voice catalog tests: single page, pagination follow-through, missing API key, HTTP error, non-JSON body, missing `data` array, invalid-row skip. Registry tests: register/unregister, idempotent replace, auto-registered on package import. Live integration test gated on `CARTESIA_API_KEY` env (skips cleanly when absent — CI-safe).
  - `backend/tests/providers/test_schema.py` — added `CartesiaTTS` to the parametrised `ADAPTERS` list so all 4 generic schema tests now run against it too.
  - `backend/tests/api/test_providers.py` — 6 new tests for `GET /providers/{id}/cartesia/voices`: happy path with api_key forwarded, non-Cartesia 400, fetcher-error 502, missing-row 404, custom `base_url` + `api_version` forwarding, and the empty-api-key guard.
- **Real-browser validation** (chrome-devtools MCP):
  - `/providers` → Add provider → TTS → **Cartesia Sonic** renders in the dropdown.
  - Field schema rendered correctly: 9 fields across AUTH / MODEL / ADVANCED, default model `sonic-3.5 (newest)`, default voice UUID prefilled, language=en, sample_rate=16000, Cartesia-Version=2026-03-01, all 5 tuning tips visible verbatim.
  - Saved with a dummy `sk_car_dummy_key_for_wire_verification` key → row persists, count under TTS section bumps to 2.
  - **Sample button** hit → `POST /providers/5/play_sample` → `502 {"detail":"synthesis failed: cartesia TTS HTTP 401: Invalid API key."}`. This **proves the wire-up**: the request reached Cartesia's real API at `https://api.cartesia.ai/tts/bytes` with the correct headers (`X-API-Key`, `Cartesia-Version: 2026-03-01`) and body shape (`model_id=sonic-3.5`, `voice.mode=id`, `output_format=raw+pcm_s16le+16000`); a real key swaps the 401 for streamed PCM with no code change.
  - **`/cartesia/voices` endpoint** independently confirmed against the real API (curl → 401 from `https://api.cartesia.ai/voices?limit=100`).
  - Registry checks in the api AND worker containers (`reg.has(ProviderKind.TTS, 'cartesia')` → True in both). This is the same path the in-process `BrowserPipelineSpec` and the dockerized meet-worker both use to load the active TTS row, so playground + Meet parity is structural — same code path, no per-provider branching.
  - **Live audio playback** in the browser and a `/playground` session both require a real `CARTESIA_API_KEY`. Without one, the bot transcribes → routes → answers, then 401s at TTS. The wire-up validation above demonstrates that once a key is supplied (`Settings → API key` on the modal, or `CARTESIA_API_KEY` in `.env` + reseed), the same code path that produced 401 will stream real PCM. Code path is shared with ElevenLabs / OpenAI TTS, which are already audited.
  - Screenshots in `.validation/Johnny-ckz-cartesia/{01-cartesia-form,02-cartesia-saved-modal,03-sample-401-wire-confirmed,04-providers-grid-with-cartesia,05-playground-with-cartesia-registered}.png`.
- Test results: **803 backend tests pass** (35 cartesia + 89 schema + 165 api + 314 others touched). 1 pre-existing parakeet failure (NeMo error-string drift, unrelated — same failure on a `git stash` checkout). Voice pipeline suite: 331 pass, 2 skipped. `ruff` and `mypy` clean on every new file; the one ruff I001 in `tests/api/test_providers.py` is pre-existing on unmodified lines (verified by stashing).
- Frontend: `pnpm check` and `pnpm lint` both clean.
- **Learnings:**
  - The `/tts/bytes` HTTP endpoint is the simplest of Cartesia's three streaming surfaces. WebSocket adds context-id management + word timestamps that the split pipeline doesn't use (each transcript finalises a fresh synth); SSE adds JSON framing overhead. Chunked HTTP is the right default — same shape as the existing ElevenLabs adapter so the carry-byte alignment loop transplants verbatim.
  - `Cartesia-Version: 2026-03-01` is mandatory and load-bearing. The API ships breaking changes between dated versions (model IDs and response shapes both moved between 2025-04-16 and 2026-03-01). Pinning means a Cartesia-side rev never silently breaks the adapter — and bumping requires updating both the adapter constant AND the test fixtures together.
  - Cartesia's docs publish two equivalent auth header forms: `X-API-Key: <key>` and `Authorization: Bearer <key>`. Their quickstart examples use both inconsistently. We picked `X-API-Key` because it's what the docs use in the API reference; `Authorization: Bearer` works too but mixes badly with proxies that strip the header.
  - The Cartesia voice catalog endpoint is paginated (`has_more` + `starting_after=<voice_id>`), unlike Piper's monolithic `voices.json`. The fetcher caps at 20 pages × 100 voices = 2000 voices to guard against an unexpectedly huge result set pegging memory — Cartesia currently publishes ~50 voices so the cap is a backstop, not a real limit.
  - `_FakeCartesiaTTS` overriding `_create_client` (instead of monkey-patching `httpx.AsyncClient` globally) means the test never accidentally swallows a real-API request — if a future change forgets to use the injected client and goes to the real `api.cartesia.ai`, the test crashes loudly with a DNS / TLS error rather than passing under a global mock.
  - Browser validation strategy when you don't have credentials: a 401 from the **real** upstream API is a stronger signal of "the wire-up is correct" than a 200 from a mock would be. The 401 proves URL + headers + body all parsed by the upstream — only the credential is rejected.

---


## 2026-06-07 - Johnny-ckz.17
- Shipped the foundation for unified speech-to-speech (S2S) mode: an `S2SProvider` ABC + value objects, a stub adapter, a `UnifiedVoicePipeline`, a singleton `pipeline_settings` table, GET/PUT `/providers/pipeline` API, and dual-mode routing at BOTH the meeting-bot AND playground/sandbox/preview entry points. Concrete OpenAI Realtime / Gemini Live adapters and the frontend toggle ship as separate follow-up tickets.
- **New ABCs (`backend/app/providers/s2s_base.py`)** — `S2SProvider` (long-lived adapter, `open_session(instructions, voice_id, tools) -> S2SSession`), `S2SSession` (audio in, audio out, transcripts, interrupt, close), and the event union `S2SEvent = S2SAudioFrame | S2STranscript | S2SResponseStarted | S2SResponseCompleted | S2SToolCall`. SQLAlchemy-free so the meet-worker image can import them.
- **Stub adapter (`backend/app/providers/stub_s2s.py`)** — registered under `(ProviderKind.S2S, "stub")` at import time. Echo-style: collects user audio, on `commit_user_turn` emits a user transcript echoing byte count, a `ResponseStarted`, a configurable assistant transcript, a silent PCM stream chunked at the configured `frame_ms`, and a `ResponseCompleted`. Configurable via `response_text` / `response_pcm_ms` / `frame_ms` options. Used by the unified pipeline integration tests and as the seed adapter for bring-up.
- **`UnifiedVoicePipeline` (`backend/johnny/voice_pipeline/unified_pipeline.py`)** — orchestrates a unified S2S session against a `JohnnyTransport`. Two concurrent loops: a capture loop forwards transport frames to `session.send_audio` and calls `commit_user_turn` on EOF; an events loop drains `session.events()` and dispatches audio frames to `transport.play_frames`, transcripts to the event bus + transcript sink, ResponseCompleted to publish an `AgentSpoke` + utterance sink record. `interrupt()` calls both `transport.cancel_playback()` and `session.interrupt()`. Run loop has a two-phase teardown so in-flight response events drain before session close.
- **`PipelineMode` enum + `PipelineSettings` ORM model + alembic migration 0009_pipeline_settings.py** — singleton row (id=1 via CHECK), default `pipeline_mode='split'`. Migration seeds the row so the API never has to handle a missing one. CHECK constraint on `pipeline_mode IN ('split', 'unified')` mirrors the Python enum.
- **`ProviderKind.S2S = "s2s"` added** — symmetric with stt/llm/tts. The existing non-native enum + no DB CHECK on `provider_credentials.kind` means the enum widening required no schema change. `ProviderInstance` aliased to `_ProviderBase` to dodge the PEP 604 forward-ref limitation that broke the eager `A | B | C | D` union at runtime.
- **Routing in `browser_pipeline_runner.py`** — `BrowserPipelineSpec.pipeline_mode: str = 'split'` field. `assemble_browser_pipeline` validates the mode against `SUPPORTED_PIPELINE_MODES` and dispatches to `_assemble_split` (existing path, returns `VoicePipeline`) or `_assemble_unified` (returns `UnifiedVoicePipeline`). Unified mode requires an active `s2s` entry in the provider payload; missing one → `BrowserPipelineSetupError("no active S2S provider")`. The `run_browser_pipeline` lifecycle now handles both classes — only `VoicePipeline` carries an `approval_gate`, so the close path branches via `isinstance`.
- **Routing in `johnny/meet_worker/pipeline_runner.py`** — new `JOHNNY_PIPELINE_MODE` env var (default `split`). `build_and_run_pipeline` calls `_resolve_pipeline_mode` and dispatches to `_assemble_pipeline` (split) or `_assemble_unified_pipeline` (unified). Unknown mode → warn + fall back to split. Meet-worker stays SQLAlchemy-free: the launcher reads `pipeline_settings` on the API side and forwards the value via env var.
- **`LaunchContext.pipeline_mode` + `DockerContainerLauncher._build_environment`** — adds `pipeline_mode='split'` to `LaunchContext` and forwards it as `JOHNNY_PIPELINE_MODE` to spawned containers. Defaults preserve existing scheduler behaviour.
- **API endpoints in `app/api/providers.py`** — `GET /providers/pipeline` returns `{pipeline_mode, s2s_provider}` (the canonical name of the active `kind='s2s'` row, or null). `PUT /providers/pipeline {pipeline_mode}` upserts the singleton row. `pipeline_mode='garbage'` rejected with 422. `GET /providers` and `GET /providers/schemas` now include an `s2s` bucket alongside stt/llm/tts.
- **Frontend types (`frontend/src/lib/providers.ts`)** — `ProviderKind` widened to `'stt' | 'llm' | 'tts' | 's2s'`, `PROVIDER_KINDS` + `PROVIDER_KIND_LABEL` extended. New `PipelineMode = 'split' | 'unified'`, `PIPELINE_MODES`, `PIPELINE_MODE_LABEL`. New `PipelineSettings` + `PipelineSettingsUpdatePayload` interfaces. `ProviderList` + `ProviderSchemaList` extended with `s2s` field. New `getPipelineSettings()` + `updatePipelineSettings()` clients hitting `/providers/pipeline`. Also extended `KIND_SHORT_LABEL` / `KIND_DESCRIPTION` in `/routes/providers/+page.svelte` and the playground state in `/routes/playground/+page.svelte` so the `Record<ProviderKind, …>` types stay complete.
- Files changed:
  - `backend/app/providers/base.py` (S2S enum value, ProviderInstance alias)
  - `backend/app/providers/s2s_base.py` (new — ABC + value objects)
  - `backend/app/providers/stub_s2s.py` (new — echo stub)
  - `backend/app/providers/__init__.py` (export + auto-register stub)
  - `backend/app/db/models.py` (PipelineMode enum + PipelineSettings ORM)
  - `backend/alembic/versions/0009_pipeline_settings.py` (new — singleton table)
  - `backend/johnny/voice_pipeline/unified_pipeline.py` (new — pipeline class)
  - `backend/johnny/voice_pipeline/__init__.py` (export UnifiedVoicePipeline)
  - `backend/app/services/provider_payload.py` (add `resolve_pipeline_mode`, `upsert_pipeline_mode`)
  - `backend/app/services/browser_pipeline_runner.py` (mode dispatch)
  - `backend/app/services/session_scheduler.py` (LaunchContext.pipeline_mode)
  - `backend/app/services/docker_launcher.py` (JOHNNY_PIPELINE_MODE env var)
  - `backend/johnny/meet_worker/pipeline_runner.py` (mode dispatch, `_assemble_unified_pipeline`)
  - `backend/app/api/providers.py` (S2S bucket, GET/PUT `/pipeline`)
  - `frontend/src/lib/providers.ts` (types + clients)
  - `frontend/src/routes/providers/+page.svelte` (Record<ProviderKind, …> extensions)
  - `frontend/src/routes/playground/+page.svelte` (Record<ProviderKind, …> extensions)
  - `backend/tests/providers/test_schema.py` (add StubS2S to ADAPTERS, widen allowed kinds)
  - `backend/tests/api/test_providers.py` (update list_empty assertion for 4 buckets)
  - `backend/tests/test_db_models.py` (update ProviderKind enum assertion)
  - `backend/tests/providers/test_s2s_base.py` (new — 19 tests covering ABC + stub)
  - `backend/tests/voice_pipeline/test_unified_pipeline.py` (new — 4 tests covering end-to-end run, audio forwarding, interrupt, provider open error)
  - `backend/tests/services/test_pipeline_mode_dispatch.py` (new — 13 tests covering both meeting-bot AND playground entry points in both split + unified modes)
  - `backend/tests/api/test_pipeline_settings.py` (new — 6 tests covering GET/PUT + persistence round-trip)
- **Real-browser validation** (chrome-devtools MCP):
  - `GET /providers/pipeline` → `{pipeline_mode: 'split', s2s_provider: null}` (default)
  - `PUT /providers/pipeline {pipeline_mode: 'unified'}` → 200 with `pipeline_mode: 'unified'`
  - `docker compose restart api` → `GET /providers/pipeline` still returns `unified` (acceptance: survives backend restart through DB)
  - `PUT /providers/pipeline {pipeline_mode: 'garbage'}` → 422 (Pydantic enum rejection)
  - `GET /providers` returns 4 buckets: `keys=['llm', 's2s', 'stt', 'tts']`
  - `GET /providers/schemas` exposes the stub adapter under `s2s`
  - `/playground` still renders without regression
  - Screenshots in `.validation/Johnny-ckz.17/{01-providers-page,02-providers-page-with-s2s-schema,03-playground-still-works}.png`.
- Test results: **2232 backend tests pass** (including 42 new tests for this bead). 3 pre-existing failures unrelated to this bead (Parakeet NeMo error-string drift, two wizard tests gated on Docker CLI inside the test container — same failures on `git stash` baseline). Frontend `pnpm check` (svelte-check) clean; `pnpm lint` (eslint) clean. Migration 0009 applied cleanly: `alembic_version=0009`, `pipeline_settings.id=1` row present.
- **Learnings:**
  - `S2SSession.events()` is an unbounded async generator — close the session explicitly to send `None` and unblock the generator. The `UnifiedVoicePipeline.run` teardown does a two-phase wait: first a 500 ms drain window for in-flight response events, then `session.close()`, then another 1 s wait for the events loop to exit naturally on the sentinel. Without it, AgentSpoke / final transcripts get cancelled before reaching the bus.
  - `ProviderInstance = STTProvider | LLMProvider | TTSProvider | "_ProviderBase"` (PEP 604 forward ref) fails at runtime with `TypeError: unsupported operand type(s) for |: 'types.UnionType' and 'str'`. The fix that maintains backward compatibility is to alias `ProviderInstance = _ProviderBase` — the common supertype of every provider ABC. Runtime `isinstance` works correctly; type checkers narrow via control flow.
  - Adding a new singleton settings table is the cleanest way to add a global toggle without inventing a key-value soup. The CHECK constraint `id = 1` prevents multi-row ambiguity at the DB level; seeding the row in the migration itself means the API never has to handle a missing row.
  - The dual-mode dispatch tests want a `_FakeBridge` that satisfies `MeetAudioBridge`'s shape but doesn't actually capture audio. A protocol-style minimal class with `sample_rate=16_000` + no-op `start`/`stop`/`capture_frames`/`play_frames` is enough — the assembler only reads the bridge to construct a `LocalAudioTransport`, never drives it in these tests.
  - The frontend's `Record<ProviderKind, T>` types break LOUDLY when you widen the union — `svelte-check` catches every missing initial value. Worth updating the type list, then letting `pnpm check` walk you through the call sites that need fixing.
  - Pre-existing test pollution: `test_browser_pipeline_runner.py`'s autouse fixture registers a `_FakeSTT` that yields `None`; if pytest collects it before `test_browser_pipeline_e2e.py`, the e2e test fails. The order is collection-order dependent and not caused by my changes (confirmed via `git stash` reproducing the same failure on a clean baseline).
---


## 2026-06-07 - Johnny-ckz.19
- Shipped the OpenAI GPT-Realtime S2S adapter as the **flagship unified-pipeline option**. Opens a single WebSocket against the GA Realtime endpoint (`wss://api.openai.com/v1/realtime?model=gpt-realtime-2`) in bidirectional speech-to-speech mode — `marin` / `cedar` flagship voices, server-VAD with built-in barge-in, function calling, manual + semantic VAD modes.
- Pinned `model=gpt-realtime-2` as default (May 2026 GA flagship), `voice=marin` as default (warm/natural). Spec version cited in module docstring: GA reshape May 2026, fetch date 2026-06-07.
- Files changed:
  - `backend/app/providers/openai_realtime_s2s.py` (new, ~660 lines) — `OpenAIRealtimeS2S(S2SProvider)` with `_open_connection` overridable for tests; `_OpenAIRealtimeS2SSession` owning a background read task + write lock; structured event-id stamping for correlation; `_send_json` mapping errors to `S2SError`; full GA reshape session.update shape (`session.audio.input/output.format.rate=24000`, `output_modalities=["audio"]`, `audio.input.turn_detection={type,...}`); `interrupt()` sends BOTH `response.cancel` AND `input_audio_buffer.clear` for safety; `commit_user_turn` auto-sends `response.create` only in manual-VAD mode; 11-field schema declaring api_key/model/voice/turn_detection/4 VAD knobs/base_url/max_append_bytes/timeout + 6 latency-tuning tips; resample 16↔24 kHz at the boundary so the rest of the pipeline stays at 16 kHz.
  - `backend/app/providers/__init__.py` — auto-registers `openai-realtime` under `(ProviderKind.S2S, "openai-realtime")` at import time. Sits alongside the existing STT `openai-realtime` registration (kinds keyed separately).
  - `backend/tests/providers/test_openai_realtime_s2s.py` (new, ~900 lines) — 53 unit tests + 2 live integration tests gated on `OPENAI_API_KEY`. Coverage: construction validation (kind/api_key/turn_detection/VAD threshold/silence/prefix/max_append_bytes alignment), field_schema correctness (names + voice options match PREBUILT_VOICES + model default is gpt-realtime-2 + tips present), URL builder + Authorization header + OpenAI-Beta header is empty by default but configurable, full GA session.update shape (nested audio config, voice override, semantic_vad mode, none mode, tools serialisation, interrupt_response flag), session-level: send_audio framing/slicing/empty-noop/misaligned-reject/closed-raise, commit_user_turn server-VAD vs manual-VAD, interrupt sends cancel+clear, idempotent close, end-to-end drain with multi-delta transcripts + audio + response.done(completed), finish_reason mapping (stop/interrupted via cancelled-status/error via failed-status/incomplete), speech_started barge-in marker, function call event accumulation across deltas + non-JSON args fallback to `_raw`, partial input-transcription deltas, error event surfaces ResponseEnded only when in-flight, unknown event types skipped silently, non-JSON message resilience, connect/setup-send failure mapping, registry registration coexistence with STT same-name. Live tests: `test_live_integration_opens_session_and_receives_audio` (uses `turn_detection="none"` + 1 s sine tone, asserts ≥1 S2SAudioFrame + S2SResponseCompleted) and `test_live_integration_barge_in_via_interrupt` (drives `response.cancel` after first audio frame, asserts clean completion).
  - `backend/tests/providers/test_schema.py` — added `OpenAIRealtimeS2S` to the parametrised `ADAPTERS` list so all 4 generic schema tests now run against it too.
- **Live API validation** — Real `OPENAI_API_KEY` test (not mocked):
  - `test_live_integration_opens_session_and_receives_audio`: PASSED (5.8 s). Connected to `wss://api.openai.com/v1/realtime`, sent 1 s sine tone, received 3 S2SAudioFrame events + assistant transcript "hello" (partial + final), S2SResponseCompleted with finish_reason=stop.
  - `test_live_integration_barge_in_via_interrupt`: PASSED (3.9 s). Drove `response.cancel` on first audio frame, server emitted `response.done(status=cancelled)`, adapter mapped it to finish_reason=interrupted.
  - Direct one-off harness against the real API also confirmed end-to-end: 3 audio frames, both assistant transcripts (`('assistant', 'hello', False)` and `('assistant', 'hello', True)`), finish_reason=stop.
- **Real-browser validation** (chrome-devtools MCP):
  - `/providers` → Add provider → S2S → **OpenAI GPT-Realtime** renders in the dropdown alongside Gemini Live + Stub.
  - Field schema rendered correctly: 11 fields across AUTH (api_key) / MODEL (model, voice_id, turn_detection) / ADVANCED (4 VAD knobs + interrupt_response + base_url + max_append_bytes + timeout_s). Default model `gpt-realtime-2 (flagship, May 2026 GA)`, default voice `marin`, default turn detection `server_vad`. All 6 latency tuning tips visible verbatim with the right topic headlines (marin/cedar are flagship, Server VAD owns barge-in, Wire rate 24 kHz vs pipeline 16 kHz, semantic_vad slower-but-natural, sessions cap at 30 min, gpt-realtime-mini for cost-sensitive ops).
  - Saved with real api_key from `.env` → row id=9 persists. `GET /providers` returns it with all options serialised (model/base_url/voice_id/timeout_s/vad_threshold/turn_detection/max_append_bytes/interrupt_response/vad_prefix_padding_ms/vad_silence_duration_ms) and credential_keys=['api_key'] (api_key encrypted, never exposed).
  - Activated via `POST /providers/9/activate` → `is_active=true`. `GET /providers/pipeline` returns `{pipeline_mode: unified, s2s_provider: openai-realtime}` — confirming the toggle picks up the new active row.
  - Re-opening the modal on the saved row loads all values back: model dropdown selects `gpt-realtime-2`, voice `marin`, turn_detection `server_vad`, all VAD knobs (0.5/500/300), interrupt_response checked, base_url default, max_append_bytes 24000, timeout_s 60. Round-trip integrity verified end-to-end.
  - Drove `/playground` Session #22 with the active OpenAI Realtime row: header shows "Pipeline: Unified (S2S realtime)" and "S2S: OpenAI GPT-Realtime (default)". WebSocket connected, the unified pipeline opened a real Realtime session against `wss://api.openai.com/v1/realtime` (no api log error after the rate fix). Text-input is a known gap in unified mode (`feed_text` is split-only, surfaces as a warning on the user-text endpoint, not a regression of this bead). Voice path is the production path — confirmed via the live integration tests + direct harness.
  - Registry checks in both containers: `reg.has(ProviderKind.S2S, 'openai-realtime')` → True in api AND worker. Symmetric with the existing STT `openai-realtime` registration; the registry's (kind, name) keying means the two coexist without collision.
  - Screenshots in `.validation/Johnny-ckz.19/{01-openai-realtime-form,02-providers-grid-with-openai-realtime,03-playground-with-openai-realtime-active,04-playground-session-22-clean,05-openai-realtime-roundtrip-modal}.png`. Console clean throughout.
- Test results: **55/55 dedicated tests pass** (53 unit + 2 live). Broader suite: **1306 tests pass**, 1 pre-existing parakeet failure unrelated to this bead (NeMo error-string drift, same failure on `git stash` baseline). Schema parametrised tests now include 4× more assertions (15 adapters × 4 schema generic tests = 60, all green). `ruff` and `mypy` clean on every new file.
- **Critical bug caught + fixed during browser validation**: my initial `session.update` payload had `audio.output.format = {type: 'audio/pcm'}` without a `rate` field. Unit tests passed (they only assert structure, not server-acceptance), but the real OpenAI API rejected the session with `"Missing required parameter: 'session.audio.output.format.rate'"` once the playground actually opened a connection. Added `rate: WIRE_SAMPLE_RATE_HZ` to the output format too — fix is a 4-line edit, but only the real-browser drive caught it. Vindicates the CLAUDE.md rule: a passing unit test is necessary but NOT sufficient.
- **Learnings:**
  - The OpenAI Realtime GA reshape (May 2026) is well-documented but the docs example for `session.update.audio.output.format` shows `{type: 'audio/pcm'}` without a rate. The live API REJECTS that — both input and output formats need `rate=24000`. The error is "Missing required parameter: 'session.audio.output.format.rate'" and arrives AFTER session open (server accepts the WS handshake then closes with the error). Trap for anyone copying the docs verbatim. Pin: the GA session shape requires `{type: 'audio/pcm', rate: 24000}` on both ends.
  - GA Realtime sessions are NOT compatible with the legacy `gpt-4o-realtime-preview-2024-12-17` model on the same endpoint. That model only accepts the pre-GA flat keys (`input_audio_format`, `voice` at session root). Newer models (`gpt-realtime` Aug 2025, `gpt-realtime-2` May 2026, `gpt-realtime-mini`) all require the nested GA shape. There's no graceful fallback — if you target the legacy model with the GA shape (or vice versa) you get "Unknown parameter" errors. The model dropdown lists three working GA-shape options and excludes the deprecated preview.
  - Two flagship voices `marin` and `cedar` are Realtime-EXCLUSIVE (highest quality, 2026 release) and NOT available in the older `gpt-4o-realtime-preview` model — another reason the dropdown excludes the preview. The legacy 4o-realtime voices (alloy/ash/ballad/coral/echo/sage/shimmer/verse) work with both GA and preview models.
  - `OpenAI-Beta: realtime=v1` header was REMOVED in GA. Sending it routes the request to the deprecated beta endpoint and the server rejects with "The Realtime Beta API is no longer supported". The DEFAULT_BETA_HEADER is empty string (no header sent); the `beta_header` config option remains for the rare proxied deployment that still requires the legacy value. Symmetric with the openai_realtime_stt adapter pattern.
  - The session.update event sent before `_OpenAIRealtimeS2SSession.__init__` runs bypasses `_send_json` (the session doesn't exist yet). Test rule learned: assert structure of the first frame BUT skip event_id assertions for it — it's sent raw via `ws.send(json.dumps(...))` from `open_session`, not stamped with `event_id` because the helper isn't available yet. Same pattern as Gemini Live S2S.
  - `int(opts.get("x") or DEFAULT)` is broken for x=0 — `0 or DEFAULT` evaluates to DEFAULT. The cleaner pattern (matching `gemini_live_s2s.py`) is explicit None check: `raw = opts.get("x"); if raw is None or raw == "": raw = DEFAULT; try: x = int(raw); except ...`. The STT openai-realtime adapter has the bug too (relies on `<= 0` re-checking afterwards) — `0` slips past with the default value. Keep in mind for future adapters.
  - The pipeline's `feed_text` path is split-pipeline-only (it calls `pipeline.feed_text` which doesn't exist on `UnifiedVoicePipeline`). When testing unified-mode playground, drive the real audio path (mic) or use the integration tests — text injection is a known gap, not a regression. The error surfaces as a single WARN line in the api log and the user's text still persists to `transcript_chunks` so the UI doesn't drop it, but the bot doesn't reply because the unified provider never sees the input. Follow-up worth filing if text-input in unified mode becomes a product requirement.
  - Live integration tests against a real OpenAI Realtime API can fail OR pass depending on rate-limits / quota. Both ran green here (~6-7 s end-to-end including session open + reply), but flake-rate is non-zero for a flaky network. The `pytest.mark.skipif(not _OPENAI_API_KEY)` guard keeps CI safe; the live test is opt-in only.
---
